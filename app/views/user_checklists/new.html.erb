<%= form_for @user_checklist do |f| %>
	<%= f.label :user_id %>
	<%= f.collection_select :user_id, User.all, :id, :username %>

	<%= f.label :checklist_id %>
	<%= f.collection_select :checklist_id, Checklist.all, :id, :title %>

	<%= f.submit %>
<% end %>