<h1>All Checklists</h1>

<ul>
    <% @checklists.each do |checklist| %>
        <li><%= link_to(checklist.title, checklist_path(checklist))%></li>
    <% end %>
</ul>

<p>What do you want to do?</p>

<%= button_to "Create", new_checklist_path, method: :get %>
